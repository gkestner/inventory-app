{
  "framework": "nextjs",
  "installCommand": "npm ci",
  "buildCommand": "npm run migrate:deploy && npm run build",
  "crons": [
    {
      "path": "/api/admin/inventory-recommendations/recalculate?apply=1",
      "schedule": "0 4 * * *"
    }
  ]
}
